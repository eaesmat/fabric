import 'package:flutter/material.dart';

class SaraiScreen extends StatelessWidget {
  const SaraiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}